# HTML in Swift: CSS

## Introduction 

In my previous post I wrote about how I settled on PointFree's approach to HTML, which I made available via pointfree-html. pointfree-html is an excellent foundation for HTML, but it relies only stringly typed methods to add styles.

We can do so, so much better.

